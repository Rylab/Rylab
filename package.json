{
  "name": "rylab.dev",
  "version": "0.0.1",
  "engines": {
    "node": ">=10"
  },
  "description": "rylab.dev website source code.",
  "main": "index.js",
  "scripts": {
    "build": "next build",
    "export": "NODE_ENV=production next export",
    "lint": "eslint . && tslint -c tslint.json '**/*.ts?(x)'",
    "start": "NODE_ENV=production next start",
    "test": "NODE_ENV=test jest",
    "test:watch": "NODE_ENV=test jest --watch"
  },
  "author": "Ryan D LaBarre",
  "license": "ISC",
  "dependencies": {
    "next": "^10.0.5",
    "react": "^17",
    "react-dom": "^17"
  }
}
